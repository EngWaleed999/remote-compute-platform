global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'user-service'
    static_configs:
      # Since we are using Docker Compose, Prometheus can reach the backend 
      # using the service name 'user-service' and port '3001'.
      - targets: ['user-service:3001']
